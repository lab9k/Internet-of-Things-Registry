import { defineMessages } from 'react-intl';

export const scope = 'IoTRegister.components.MapInteractive';

export default defineMessages({
  about: {
    id: `${scope}.about`,
    defaultMessage: 'About this register',
  },
});
